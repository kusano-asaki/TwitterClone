<h1>つぶやき一覧</h1>
<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr>
<% @tweets.each do |tweet| %>
  <tr>
    <td><%= tweet.title %></td>
    <td><%= tweet.content %></td>
    <td><%= link_to '詳細を確認' , tweet_path(tweet.id)%></td>
    <td><%= link_to 'つぶやきを編集する' , edit_tweet_path(tweet.id)%></td>
  </tr>
<% end %>
</table>
<%= link_to '新しくつぶやく', new_tweet_path %>
